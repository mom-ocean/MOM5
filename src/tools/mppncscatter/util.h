#ifndef MPPNCSCATTER_UTIL_H
#define MPPNCSCATTER_UTIL_H

#include "math.h"

/*-------------------------------------------------------------------*/
#define EVEN(X) ( ((X) % 2) ? 0 : 1 )
#define MAX(A,B) ( ((A) > (B)) ? (A) : (B) )
#define ODD(X) ((X) % 2)

#endif /* MPPNCSCATTER_UTIL_H */